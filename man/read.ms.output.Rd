\name{read.ms.output}
\alias{read.ms.output}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{Convert Raw \code{ms} Output to List
%%  ~~function to do ... ~~
}
\description{A function, written by Rick Hudson and distributed with the source code for \code{ms}, that converts raw \code{ms} output into a list.
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
read.ms.output(txt = NA, file.ms.output = NA)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{txt}{Name of internal R object generated by ms
%%     ~~Describe \code{txt} here~~
}
  \item{file.ms.output}{\code{character}  file name of saved ms output
%%     ~~Describe \code{file.ms.output} here~~
}
}
\details{
This function will parse the output of ms, generating a list of values described below.  Note that, as commonly used in this package, the file.ms.output option is not used. As shown below (see example), use of \code{txt=} is optional; the name of the r object to be input will suffice.
%%  ~~ If necessary, more details than the description above ~~
}
\value{ List of 7.  Each item of length nreps, where nreps is the number of simulations performed.
%%  ~Describe the value returned
%%  If it is a LIST, use
\item{segsites}{\code{integer} - number of segregating sites}
\item{gametes }{\code{list} - arrays of gametes generated}
\item{probs }{\code{integer} - probability of data if both -s and -t are specified}
\item{times }{\code{matrix} - TMRCA and total tree length (if -L specified)}
\item{positions }{\code{list} - relative positions of segregating sites}
\item{nsam }{\code{integer} - number of sequences generated}
\item{nreps }{\code{integer} - number of simulations}
%% ...
}
\references{
{Hudson, R. R.,2002. Generating samples under a Wright-Fisher neutral model. Bioinformatics \strong{18}: 337-8}

Hudson, RR.  ms documentation \url{https://webshare.uchicago.edu/users/rhudson1/Public/ms.folder/msdoc.pdf?ticket=t_DunQ7c99}
}
\author{
{R. R. Hudson \email{rrhudson@uchicago.edu}}  

{Bruce J. Cochrane \email{cochrabj@miamioh.edu}}
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
\code{\link[phyclust]{ms}}
}
\examples{
# An example in which segregating sites and theta are both specified

set.seed(123)
dat <-ms(nsam=100,nreps=2,opts=c("-s 51 -t 10 -L -T"))
dat.ms <-read.ms.output(dat)
str(dat.ms)

% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.

\keyword